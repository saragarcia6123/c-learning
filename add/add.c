#include <stdio.h>
#include <stdlib.h>

int add(int a, int b) {
	return a + b;
}

int multiply(int a, int b) {
	return a * b;
}

int main(int argc, char *argv[]) {
	char inputA[20] = { 0 };
	char inputB[20] = { 0 };

	sscanf(argv)
	

	int result = add(a, b);

	printf("%d + %d = %d\n", a, b, result);

	return 0;
}
